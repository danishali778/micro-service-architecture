name: Identity Service CI

on:
  pull_request:
    paths:
      - ".github/workflows/identity-service-ci.yml"
      - ".python-version"
      - "pyproject.toml"
      - "uv.lock"
      - "services/identity-service/**"
      - "docs/services/identity-service/**"
  push:
    branches: [main]
    paths:
      - ".github/workflows/identity-service-ci.yml"
      - ".python-version"
      - "pyproject.toml"
      - "uv.lock"
      - "services/identity-service/**"
      - "docs/services/identity-service/**"

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_USER: identity_service
          POSTGRES_PASSWORD: identity_service
          POSTGRES_DB: identity_service_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_TEST_URL: postgresql+psycopg://identity_service:identity_service@127.0.0.1:5432/identity_service_test
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
        with:
          version: "0.11.16"
          enable-cache: true
      - run: uv python install 3.13
      - run: uv sync --all-packages --group dev --locked
      - run: uv run ruff format --check services/identity-service
      - run: uv run ruff check services/identity-service
      - run: uv run mypy --config-file services/identity-service/pyproject.toml services/identity-service/app services/identity-service/scripts
        env:
          MYPYPATH: services/identity-service
      - run: uv run pytest
        working-directory: services/identity-service
        env:
          DATABASE_TEST_URL: postgresql+psycopg://identity_service:identity_service@127.0.0.1:5432/identity_service_test
          PYTHONPATH: .
      - run: uv run python services/identity-service/scripts/validate_openapi.py
      - run: docker build -f services/identity-service/Dockerfile .
